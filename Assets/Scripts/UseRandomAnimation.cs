using UnityEngine;

public class UseRandomAnimation : MonoBehaviour
{
    public GameObject[] objectList;

    void Start()
    {
        // Transform parent = GetComponent<Transform>();

        // GameObject sprite = Instantiate(
        //     objectList[Random.Range(0, objectList.Length)],
        //     parent.position,
        //     Quaternion.identity,
        //     parent);

        // sprite.transform.localScale = new Vector3(11, 11, 0);
    }
}
